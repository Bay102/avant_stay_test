// import { useQuery } from '@apollo/client';
// import { AllRegions } from './queries';

export const Test = async () => {
  //   const { loading, error, data } = await useQuery(AllRegions);
  //   console.log(data, error);
};
